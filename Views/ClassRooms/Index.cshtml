@model IEnumerable<Classroom>

@ViewData["Title"] = "Add new record class room"

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Add new class room record</h1>
        </p>
        <table class="table">
            <thead>
                <tr class="text-center">
                </tr>
            </thead>
        </table>
    </div>
</div>